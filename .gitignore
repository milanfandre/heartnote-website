# dependencies
node_modules/

# generated screenshots (local review only)
temporary screenshots/

# scratch scripts
_*.mjs

# os
.DS_Store

# heavy source photos — commit only the web-optimized versions
brand_assets/hf_*.png
brand_assets/hf-grandma.png

# full-length master songs — commit only the web-optimized /audio samples
audio/source/

# internal reference docs (contain AI prompts) — never commit / never web-accessible
brand_assets/*.pdf

# secrets — never commit real env files
.env
.env.local
.env*.local
.vercel
env/heartnote.env
