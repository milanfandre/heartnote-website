# dependencies
node_modules/

# generated screenshots (local review only)
temporary screenshots/

# scratch scripts
_*.mjs

# os
.DS_Store

# heavy source photos — commit only the web-optimized versions
brand_assets/hf_*.png
brand_assets/hf-grandma.png

# full-length master songs & voice sources — commit only the web-optimized clips
audio/source/
audio/Avatars/

# internal reference docs (contain AI prompts) — never commit / never web-accessible
brand_assets/*.pdf

# heavy original photos — commit only the optimized occasion-*.jpg versions
brand_assets/source/

# secrets — never commit real env files
.env
.env.local
.env*.local
.vercel
env/heartnote.env

# heavy original review photos — the cropped review-*.jpg versions are committed
brand_assets/grandma(2).png
brand_assets/lady-on-beach.png
brand_assets/old-couple.png
brand_assets/wedding-man-and-woman.png
